import { HelloServer } from '@t3-stack/libs/server';

export default function Index() {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./index.css file.
   */
  return (
    <main>
      hello world
      <HelloServer />
    </main>
  );
}
